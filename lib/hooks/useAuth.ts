// lib/hooks/useAuth.ts
// Convenience re-export of the useAuth hook from AuthContext.
// Import from here in all components so the import path stays consistent.

export { useAuth } from "@/lib/context/AuthContext";
